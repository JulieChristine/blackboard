{{#if updateForm}}
  <h4>Update {{announcement.author}}'s anncouncement</h4>
    <div>
      <form {{action 'update' announcement on="submit"}}>
        <div class="form-group">
          <label for="date">Date: </label>
          {{input type="date" value=announcement.date id="date" placeholder="yyyy-mm-dd" required=true}}
        </div>

        <div class="form-group">
          <label for="content">Content: </label>
          {{input value=announcement.content id="content" placeholder=announcement.content required=true}}
        </div>

        <div class="form-group">
          <label for="author">Author: </label>
          {{input value=announcement.author id="author" placeholder=announcement.author required=true}}
        </div>
        <!-- save button -->
        <button type="submit">Save</button>
      </form>
    </div>
  {{else}}
    <button {{action 'updateForm'}}>Update</button>
{{/if}}

<button {{action 'delete' announcement}}>Delete</button>
